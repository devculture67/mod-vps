# Fail2ban filter for nginx bot API abuse
[Definition]
failregex = ^<HOST> -.*"(GET|POST) /api/.*" (401|403|429|500)
            ^<HOST> -.*"(POST) /bot2/api/exec.*" (401|403)
            ^<HOST> -.*"(POST) /bot2/api/file.*" (401|403)
ignoreregex =